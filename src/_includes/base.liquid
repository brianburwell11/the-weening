---
title: The 'Weening
css:
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
      {% if movie %}{{ movie.id }}{% else %}{{ title }}{% endif %}
    </title>
    <meta name="description" content="The Weening">
    <meta name="author" content="The Weening">

    <link rel="icon" type="image/png" href="/assets/img/weening-skull.png">

    <!-- Preconnect to external domains -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">

    <!-- Styles -->
    <link rel="stylesheet" href="/assets/css/base.css">
    <link rel="stylesheet" href="/assets/css/modal.css">
    {% if css %}
      {% for stylesheet in css %}
        <link rel="stylesheet" href="/assets/css/{{ stylesheet }}">
      {% endfor %}
    {% endif %}
  </head>

  <body>
    {% unless movie %}
      {% include '_header.liquid' %}
    {% endunless %}
    <main>
      {{ content }}
    </main>

    {{ scripts }}
    <script src="/assets/js/modal.js"></script>
  </body>
</html>
